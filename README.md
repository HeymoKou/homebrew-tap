# homebrew-tap

Homebrew tap for [cbar](https://github.com/HeymoKou/cbar).

```bash
brew install heymokou/tap/cbar
brew services start heymokou/tap/cbar   # run + start at login
```

macOS only. Builds from source (no signing/notarization needed).
